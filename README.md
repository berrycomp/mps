# MPS

Multi Processing Scaler (MPS) is Tileline's standalone CPU scheduling and SIMD kernel crate.

## Highlights

- topology-aware worker placement
- lock-free scheduler and overlapped physics dispatcher
- runtime-dispatched SIMD kernels with scalar fallback
- Linux affinity / priority bootstrap helpers

## Version

Current standalone tag: `v0.5.5`
